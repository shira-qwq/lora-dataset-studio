"""Analysis channels — side-channel features that do NOT participate in default clustering.

These modules compute interpretable metadata (histograms, residuals, statistics)
that can be used for sorting, filtering, outlier detection, and cluster explanation
without affecting the 16-dim UMAP/HDBSCAN clustering pipeline.
"""
